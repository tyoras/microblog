<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content , :maxsize => "20x4" %>
  </div>
  <div class="actions">
    <%= f.submit "Envoyer" %>
  </div>
<% end %>