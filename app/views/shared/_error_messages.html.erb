<% if object.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(object.errors.count, "erreur") %> 
      <% if object.errors.count == 1 %>
        a
      <% else %>
        ont
      <% end %> empêché la sauvegarde de la donnée <%= object.class.to_s.underscore.humanize.downcase %>:</h2>
    <% if object.errors.count == 1 %>
      <p>Il y a un problème avec le champ suivant:</p>
    <% else %>
      <p>Il y a des problèmes avec les champs suivants:</p>
    <% end %>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>